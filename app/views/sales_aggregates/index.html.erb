<h1>Listing sales_aggregates</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sales_aggregates.each do |sales_aggregate| %>
  <tr>
    <td><%= link_to 'Show', sales_aggregate %></td>
    <td><%= link_to 'Edit', edit_sales_aggregate_path(sales_aggregate) %></td>
    <td><%= link_to 'Destroy', sales_aggregate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sales aggregate', new_sales_aggregate_path %>
